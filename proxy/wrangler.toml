name = "cybus-proxy"
main = "src/worker.ts"
compatibility_date = "2025-01-01"
compatibility_flags = ["nodejs_compat"]

[observability]
enabled = true

# Routes — update with your actual Cloudflare zone/domain after `wrangler deploy`
# routes = [{ pattern = "proxy.yourdomain.com/*", zone_name = "yourdomain.com" }]
